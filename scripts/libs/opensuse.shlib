#!/bin/bash

set -e
set -u
set -o pipefail

MODULE_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
. ${MODULE_DIR}/color.shlib
. ${MODULE_DIR}/say.shlib

function zyp_inst {
  local pkg_name=${1}
}
